print(tuple(range(5)))
print(tuple(range(2,6)))
print(list(range(5,2,-1)))
# range produces something that behaves like tuples; it doesn't generate all the elements at once;
# rather it generates the first element, after which subsequent elements can be generated.
